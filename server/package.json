{
  "name": "movie-rating-server",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "node src/index.js",
    "start": "node src/index.js",
    "prisma:generate": "prisma generate",
    "prisma:migrate": "prisma migrate dev --name init"
  },
  "dependencies": {
    "@prisma/client": "^5.14.0",
    "express": "^4.18.2"
  },
  "devDependencies": {
    "prisma": "^5.14.0"
  }
}
